@using Lpp.QueryComposer

<script type="text/html" id="e_@ModelTermsFactory.DrugClassID">
    <p class="bold center operator" data-bind="text:Global.Helpers.GetEnumString(Dns.Enums.QueryComposerOperatorsTranslation, Operator()), visible:$index() > 0"></p>    
    <div class="row row-noborder">
        <div class="col-sm-2 bold"><h4 id="lblDrugClass" class="TermHeader bold">Drug Class<em data-bind="visible:Design.Locked" style="color:red">*</em></h4></div>
        <div class="col-sm-8">
            <button id="btnAddDrugClass" style="margin-bottom:10px;" class="btn btn-primary" data-bind="click: function(data, event) {$component.OpenCodeSelector(data, Dns.Enums.Lists.DrugClass, false, event);}"><span class="glyphicon glyphicon-pencil"></span> Select Drug Class</button>
            <p data-bind="visible:Values == null || Values().CodeValues == null || Values().CodeValues().length == 0">&lt;None Selected&gt;</p>
        </div>
        <div class="col-sm-2">
            <button id="btnDeleteDrugClassTerm" class="btn btn-danger pull-right" data-bind="click: $component.onDeleteTermFromCriteria.bind($component, $data, $parent), visible: $component.Options.IsTemplateEdit || !Design.Locked()"><span class="glyphicon glyphicon-minus"></span> Remove</button>
            <label class="design-lock" data-bind="visible: $component.Options.IsTemplateEdit"><input type="checkbox" data-bind="checked: Design.Locked" /> Locked</label>
        </div>
    </div>
    <div class="row row-noborder">
        <div class="col-sm-12">
            <!-- ko if: Values().Codes != null && Values().Codes.length > null -->
            <strong>Selected:</strong><span data-bind="visible:Values != null && Values().Codes != null && Values().Codes().length > 0, text:Values().Codes().join()"></span>
            <!-- /ko -->
            <div id="lblCodes" data-bind="visible:Values != null && Values().CodeValues != null && Values().CodeValues().length > 0">
                <!-- ko if: Values().CodeValues != null && Values().CodeValues().length > null -->
                <ul data-bind="foreach: Values().CodeValues()">
                    <li>
                        <span style="font-weight:bold;" data-bind="text: Code"></span> - <span data-bind="text: Name"></span>
                    </li>
                </ul>
                <!-- /ko -->
            </div>
        </div>
    </div>
</script>
