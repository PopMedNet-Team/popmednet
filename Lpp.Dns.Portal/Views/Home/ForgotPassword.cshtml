@{
    ViewBag.Title = "Forgot Password?";
    Layout = "~/Views/_DialogLayout.cshtml";
}

@section Head {
<script src="~/js/Home/ForgotPassword.js"></script>
}
<p><strong>INSTRUCTIONS:</strong> Please enter your Username or Email. A new password will be sent to your registered email on clicking "Submit".</p>
<form role="form">
    <fieldset>
        <div class="row">
            <div class="col-sm-6 form-group">
                <label for="txtUserName">User Name</label>
                <input type="text" id="txtUserName" name="username" class="form-control" data-bind="value: UserName" />
            </div>
        </div>
        <div class="row">
            <div class="col-sm-6 form-group">
                <strong>OR</strong>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-6 form-group">
                <label for="txtEmail">Email</label>
                <input type="email" id="txtEmail" class="form-control" name="email" data-bind="value: Email" />
            </div>
        </div>
    </fieldset>
</form>
<nav class="footer">
    <button id="btnSubmit" class="btn btn-success btn-sm" data-bind="click: Save, enabled: Email() !='' || UserName() != ''"><img src="@this.ResourceUrl("/images/Save24.png")" alt="Submit" />Submit</button>
    <button id="btnCancel" class="btn btn-warning btn-sm" data-bind="click: Cancel"><img src="@this.ResourceUrl("/images/Cancel24.png")" alt="Cancel" />Cancel</button>
</nav>